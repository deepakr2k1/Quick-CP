#include <bits/stdc++.h>
using namespace std;
#define FastIO ios_base::sync_with_stdio(false),cin.tie(NULL);
#define endl "\n"
#define rep(i,a,b) for(ll i=a;i<=b;i++)
#define irep(i,a,b) for(ll i=a;i>=b;i--)
#define ip(V,n) vector<ll> V(n); for(ll _i=0;_i<n;_i++) cin>>V[_i]
#define op(V,sep) for(ll _i=0;_i<V.size();_i++) cout<<V[_i]<<sep
#define ll long long int
#define MOD 1000000007
//.............. User: bugger_   (Not to touch) ....................

void solve() {
    int n,q;
    cin>>n>>q;
    ip(A,n);
    while(q--) {
        int l,r;
        cin>>l>>r;
    }

}

int main() {
    FastIO
    // int t;
    // cin>>t;
    // rep(tt,1,t) {
        solve();
    // }
}